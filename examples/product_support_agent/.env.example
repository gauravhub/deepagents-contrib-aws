# === LLM Provider (required — at least one) ===
ANTHROPIC_API_KEY=sk-ant-...

# === AWS (required) ===
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_SESSION_TOKEN=...  # only for temporary credentials (STS/SSO)

# === S3 Backend (required) ===
S3_BACKEND_BUCKET=my-bucket
S3_BACKEND_PREFIX=product-support/

# === Summarization (optional) ===
# Trigger summarization at this fraction of context window (default: 0.10)
# Lower = triggers sooner, saving conversation history to S3 faster
# SUMMARIZATION_TRIGGER=0.10
# SUMMARIZATION_KEEP=0.05

# === LangSmith Tracing (optional) ===
# LANGSMITH_API_KEY=lsv2_...
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_PROJECT=product-support-agent
